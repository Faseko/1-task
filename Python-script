a = float(input())
b = float(input())
c = float(input())

class Sum():
		def __init__(self, X=180):
			self.X = X
		def check(a, b, c):
			if a + b + c == self.X:
				print("Верно")
			else: 
				print("Не верно")
